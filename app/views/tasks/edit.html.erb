<h1>Edit: <%= @task.title %></h1>

<%= form_with(model: @task, url: task_path(@task), method: "patch") do |t| %>
  <%= t.label :title %>
  <%= t.text_field :title %>
  <%= t.label :details %>
  <%= t.text_field :details %>
  <%= t.submit %>
<% end %>
